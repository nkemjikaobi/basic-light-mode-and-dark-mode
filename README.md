# basic-light-mode-and-dark-mode

## Getting Started

First, install the packages:

```bash
npm run install
# or
yarn install
```

First, run the development server:

```bash
npm run start
# or
yarn start
```

Then follow the commands to run on preferred device
